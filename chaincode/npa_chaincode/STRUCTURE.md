# Структура проекта NPA Chaincode

## Описание файлов

### 📄 `chaincode.py` - Основной модуль

**Назначение:**
Основной файл содержит бизнес-логику chaincode и реализует все функции для работы с задачами и документами.

**Основные компоненты:**
- Класс `NPAChaincode` - главный класс chaincode
- Методы:
  - `create_task()` - создание новой задачи
  - `update_task_status()` - обновление статуса задачи
  - `add_document_version()` - добавление версии документа
  - `get_document_versions()` - получение всех версий документа
  - `get_task()` - получение задачи по ID
- Функция `invoke_chaincode()` - точка входа для обработки вызовов chaincode

**Зависимости:**
- Использует `StateManager` из `state.py` для работы с ledger
- Использует функции валидации и форматирования из `utils.py`

---

### 📄 `state.py` - Управление состоянием

**Назначение:**
Модуль для работы с состоянием в Hyperledger Fabric ledger. Инкапсулирует все операции чтения и записи данных.

**Основные компоненты:**
- Класс `StateManager` - менеджер состояния
- Методы:
  - `get_state()` - получение значения по ключу
  - `put_state()` - сохранение значения
  - `delete_state()` - удаление значения
  - `get_state_by_range()` - получение диапазона значений
  - `create_composite_key()` - создание составного ключа
  - `get_state_by_partial_composite_key()` - получение по частичному ключу
  - `get_history_for_key()` - получение истории изменений

**Особенности:**
- Автоматическая обработка JSON сериализации/десериализации
- Централизованная обработка ошибок
- Логирование всех операций

---

### 📄 `utils.py` - Вспомогательные функции

**Назначение:**
Содержит вспомогательные функции, константы и утилиты для работы chaincode.

**Основные компоненты:**

**Константы:**
- `TASK_STATUS_*` - статусы задач
- `VALID_TASK_STATUSES` - список допустимых статусов
- `KEY_PREFIX_*` - префиксы для ключей

**Функции валидации:**
- `validate_status()` - проверка валидности статуса
- `validate_task_data()` - валидация данных задачи
- `validate_document_version_data()` - валидация данных версии документа

**Функции форматирования:**
- `format_response()` - стандартное форматирование ответа

**Утилиты:**
- `create_task_key()` - создание ключа для задачи
- `create_document_key()` - создание ключа для документа
- `sanitize_string()` - очистка строковых значений
- `get_current_timestamp()` - получение текущей временной метки
- `parse_metadata()` - парсинг метаданных из JSON
- `merge_dicts()` - объединение словарей

---

### 📄 `__init__.py` - Инициализация пакета

**Назначение:**
Инициализирует пакет и экспортирует основные классы и функции для удобного импорта.

**Экспортируемые компоненты:**
- `NPAChaincode` - основной класс chaincode
- `StateManager` - менеджер состояния
- `validate_status` - функция валидации статуса
- `format_response` - функция форматирования ответа

**Использование:**
```python
from npa_chaincode import NPAChaincode, StateManager
```

---

## Взаимосвязи модулей

```
chaincode.py
    ├── использует → state.py (StateManager)
    └── использует → utils.py (валидация, форматирование)

state.py
    └── независимый модуль

utils.py
    └── независимый модуль
```

## Преимущества структуры

1. **Модульность**: Каждый файл отвечает за свою область
2. **Переиспользование**: Утилиты и StateManager можно использовать в других проектах
3. **Тестируемость**: Каждый модуль можно тестировать отдельно
4. **Поддерживаемость**: Изменения в одном модуле не затрагивают другие
5. **Читаемость**: Код легче понимать и навигировать

